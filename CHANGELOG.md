# 更新日志

本文件记录 Vibe-Manager 的版本变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> 从 `0.5.0` 开始记录。此前的开发过程保留在 `TASK.md`（按批次的历史日志），不回填到本文件。

## [0.5.1] - 2026-09-21

### 新增

- **自动更新**：启动时静默检查 GitHub Release，有新版本时顶栏与设置页出现提示；弹窗里可查看更新说明并一键下载安装（校验 SHA256、替换程序后自动重启，失败自动回退旧程序）
- **编辑器字号**：设置页新增「编辑器字号」（10–24），样张与编辑器即时生效，与编辑器字体一样持久化
- **界面字号**：设置页新增「界面字号」（12–18），保存后整个界面文字按比例缩放，与编辑器字号互不影响

## [0.5.0] - 2026-09-18

首个基线版本：把此前各批次（0.1.0–0.3.5）的成果合并为一个稳定形态，作为 Vibe-Manager 的第一个正式版本。

### 新增

**项目库**

- 扫描项目根，列出每个项目的名称、最后活动时间、git 状态（脏·净）、当前分支、最近提交摘要、未推送提交数与「N 天未动」徽标
- 快捷筛选（全部 / 有改动 / 未设上游 / 30 天未动 / 非 git）与排序（最后活动 / 名称 / 未推送数）
- 搜索匹配项目名与最近提交信息；双击项目打开文件夹

**新建项目**

- kebab-case 名称校验；逐项勾选初始化内容（README.md / .gitignore / src/ 目录 / AGENTS.md / `git init` + 首次提交）
- 自选开源协议（不加 / MIT / Apache-2.0 / BSD-3-Clause / ISC / MPL-2.0 / GPL-3.0），协议署名可填（默认取 git 全局 `user.name`）
- 内置 5 份 AGENTS.md 模板（通用工程化 / AI 代理精简版 / 前端 Web / 脚本 Python / C++ 原生），支持自建与「另存为」；模板正文中的 `{{NAME}}` 会替换为项目名

**导入项目 / 自定义实际根目录**

- 可把项目根之外的任意目录导入列表；可为项目指定「实际根目录」，用于真仓库藏在子目录里的情况
- 列表标出「导入」与「自定义根」徽标；元数据存 `%APPDATA%\vibe-pm\projects.json`，不写进项目目录

**项目备注名**

- 可为项目设置显示名，留空即恢复目录名；列表、详情标题与搜索都认备注名

**文件管理**

- 面包屑导航、前进 / 后退 / 上级 / 刷新，大图标（默认）与详细信息两种视图
- 排序（可反向）、目录内名称筛选、显示隐藏项开关、逐条目标注 git 状态；`.git` / `node_modules` / `dist` 等噪音目录默认隐藏
- 选中文件就地预览文本与图片，双击用编辑器打开
- 完整文件操作：新建文件夹、重命名（F2）、删除到回收站（Delete）、剪切 / 复制 / 粘贴（Ctrl+X/C/V）
- 拖拽移动、多选（Ctrl 点选 / Shift 范围选 / Ctrl+A）、方向键在列表内移动选中项

**内置编辑器**

- 右侧编辑器栏，独立成栏、可拖动分隔条调宽（260–800，持久化）、可折叠
- 语法高亮由 CodeMirror 6 提供，覆盖 TS/JS/JSX、JSON、Go、Python、HTML/XML、CSS/SCSS、Markdown、YAML、Rust、C/C++、Java、PHP、SQL、Shell、TOML/INI
- 行号、当前行高亮、括号匹配与自动闭合、代码折叠、多光标、`Ctrl+F` 查找替换、`Ctrl+Z/Y` 撤销重做
- 字体可配置；`Ctrl+S` 保存；未保存时标题旁有圆点，切换文件前先询问保存
- 大文件保护：> 200 KB 关闭高亮；> 2 MB 只读并停用编辑

**版本管理（基于项目自身 git）**

- 提交历史（相对时间 / 信息 / 短 SHA / 变更文件数）与提交详情（新增 / 修改 / 删除 / 重命名）
- 一键打点、硬回滚、软回滚、撤销未提交改动
- 版本对比页：列出两个版本之间变更的文件，可逐文件展开完整 diff；带行号，初始滚动定位到首个改动
- 提交工作流：文件勾选 + 状态徽标 + diff 预览 + 提交信息 + 前缀快捷键 + amend，只提交勾选的路径
- 分支与标签：切换 / 新建 / 删除 / 合并，标签创建与推送；删除未合并分支会被拦下
- revert：生成反向提交、不改写历史
- 远程同步：显示当前分支、↑↓ 计数、远程地址与代理提示，支持抓取 / 拉取 / 推送；无上游时按钮变为「推送并设为上游」
- 备份区：集中列出工具自动留下的 stash 与 `vibe-pm-backup-*` 标签，可一键应用 / 回到该备份 / 删除

**全局搜索**

- 跨项目搜索项目名 / 提交信息 / 文件名 / 文件内容；结果按项目分组，内容命中显示 `路径:行号` 并高亮关键词
- 点击结果跳进对应项目的文件视图并定位到该文件（内容命中滚到对应行）
- 项目之间并发搜索

**归档**

- 把项目打包成 zip 存入归档根并删除原目录；归档前若工作区有未提交改动，默认先打一次点
- 归档区可解压还原回原路径，支持排序（时间 / 名称 / 体积）与删除

**其它**

- 单实例保护；界面中文；日间 / 夜间主题可切换（默认日间）；记住窗口大小；配置可视化编辑
- 运行日志写入 `%APPDATA%\vibe-pm\vibe-pm.log`

### 变更

- 项目与仓库统一更名为 **Vibe-Manager**（Go module `vibe-manager`，产物 `Vibe-Manager.exe`）；
  用户数据目录 `%APPDATA%\vibe-pm\` 与保底标签前缀 `vibe-pm-backup-` 保持不变，以免读不到既有数据
- 编辑器由自研高亮换成 CodeMirror 6；前端包随之增至 ~1.1 MB（gzip ~386 KB），exe 约 12 MB
- 详情页收敛为「版本管理 / 文件 / 对比」三个模式

### 移除

- 「任务」面板从界面下线；解析逻辑 `internal/taskdoc` 与 `GetTaskBoard` 绑定保留，可随时接回
