package agents

// 内置模板。{{NAME}} 会被替换成项目名。
// 内容刻意保持"通用最小集"：只写该类型项目里普遍成立、且违反代价高的纪律。
var builtins = []Template{
	{
		ID:      "builtin-general",
		Name:    "通用工程化（推荐）",
		Builtin: true,
		Body: `# AGENTS.md — {{NAME}} 协作约定

本文件定义在本仓库内协作（尤其是让 AI 代理接手）时必须遵守的约定。在此目录下工作自动生效。

## 提交纪律（最重要）

- **改完就提交**：任何一次能跑通的改动，立刻用 git 提交，不要攒一堆未提交改动。
- 提交信息用 **Conventional Commits**：` + "`feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:`" + `，必要时加 scope，例如 ` + "`fix(parser): 修复空行解析`" + `。
- **一次提交只做一件事**，不要把无关改动混在一起。
- 提交前先跑构建或测试；不要提交明知是坏的代码。
- 破坏性改动（重命名、删文件、改接口）单独一次提交，便于回滚。

## 代码

- 动手前先读周边代码，**跟随既有风格**，不要引入自己的偏好。
- 能不加依赖就不加；确实要加就说明理由。
- 不写用不到的抽象，不为"以后可能会用"预留代码。
- 注释只写在逻辑不自明的地方，用中文。

## 文档

- ` + "`README.md`" + ` 说清：这个项目做什么、怎么跑起来。
- 有明确任务时把任务与进度写成文件（如 ` + "`TASK.md` / `PROGRESS.md`" + `），不要只留在对话里。
- **已知限制与不确定的地方要如实写出来**，不要粉饰。

## 安全与可恢复

- **不要提交密钥、令牌、密码**；放 ` + "`.env`" + ` 并在 ` + "`.gitignore`" + ` 里排除。
- 删除要可恢复（回收站或 git 历史），不要随手 ` + "`rm -rf`" + `。
- 大改动之前先留好可回退的点（提交或打标签）。

## 工作方式

- 需求不清楚就先问，不要猜着写一大片代码。
- 改动尽量小步、可回滚；大改动先写方案再动手。
- 收尾时给出**怎么验证**的具体步骤，而不是只说"做完了"。
`,
	},
	{
		ID:      "builtin-agent",
		Name:    "AI 代理精简版（给自动化执行用）",
		Builtin: true,
		Body: `# AGENTS.md — {{NAME}}

你在这个仓库里干活时遵守以下纪律。这份文件是硬约束，不是建议。

## 必须

1. **每完成一件可运行的改动就提交一次**，用 Conventional Commits（` + "`feat:` / `fix:` / `docs:` / `chore:`" + `）。
2. **动代码之前先读周边代码**，跟随既有风格与命名，不要带来自己的习惯。
3. **不确定就问**：需求有歧义时停下来问，不要猜着写一大片；来不及问就把疑问写进 ` + "`BLOCKED.md`" + ` 并跳过。
4. **不许提交密钥、令牌、密码**。
5. **交付时要给可验证的证据**（真实跑过的命令与输出），不要只说"做完了"。

## 不许

- 不许顺手重构无关代码，不许"顺手加个功能"。
- 不许为了让检查通过而放宽、跳过或伪造测试。
- 不许删除、绕过已有的安全检查（如 ` + "`--no-verify`" + `）。
- 不许在没搞清原因的情况下绕过报错。

## 收尾

- 跑一遍构建或测试，把结果贴出来。
- 说明你改了什么、没改什么、还有什么不确定。
- 已知问题如实写，不要粉饰。
`,
	},
	{
		ID:      "builtin-web",
		Name:    "前端 / Web 项目",
		Builtin: true,
		Body: `# AGENTS.md — {{NAME}} 协作约定

## 提交纪律

- **改完就提交**，用 Conventional Commits（前端常用 ` + "`feat(ui):` / `fix(style):` / `chore(deps):`" + `）。
- 一次提交只做一件事；样式改动与逻辑改动尽量分开提交。
- 提交前确认构建通过（` + "`npm run build`" + ` 或等价命令）。

## 代码

- 跟随项目现有的组件写法、目录结构与状态管理方式，不要另起一套。
- **不引新的 UI 框架 / 图标库 / 工具库**，除非确有必要并说明理由。
- 组件保持小而单一；不在组件里塞与展示无关的业务逻辑。
- 颜色、间距、字号一律用项目已有的变量 / 设计方案，不要写魔法值。
- 注释只写在逻辑不自明处，用中文。

## 样式与可用性

- 新界面要同时能看**日间与夜间**两套主题。
- 交互元素要有 hover / active / focus 状态与键盘可达性。
- 不为了好看牺牲可读性：正文对比度、点击区域（≥ 40px）要达标。
- 不用花哨渐变、玻璃拟态这类过度装饰。

## 性能

- 长列表要虚拟化或分页，不要一次渲染上千个节点。
- 大依赖要看体积影响；能按需引入就不要整包引入。
- 事件监听记得解绑，避免内存泄漏。

## 安全

- **不要提交密钥 / token**；前端密钥一律走环境变量并在 ` + "`.gitignore`" + ` 里排除。
- 渲染用户内容要转义，不要直接 ` + "`innerHTML`" + ` 拼接未处理的数据。
`,
	},
	{
		ID:      "builtin-script",
		Name:    "脚本 / Python 项目",
		Builtin: true,
		Body: `# AGENTS.md — {{NAME}} 协作约定

## 提交纪律

- **改完就提交**，用 Conventional Commits。
- 一次提交只做一件事；依赖变更（requirements / lock 文件）单独提交。
- 脚本"能跑通"才算完成，提交前实际运行一次。

## 代码

- 跟随既有风格；不确定时用 PEP 8。
- **入口清晰**：` + "`if __name__ == \"__main__\":`" + ` 或独立 CLI 入口，不要一导入就执行副作用。
- 路径用 ` + "`pathlib`" + `，不要拼字符串；文件读写显式指定编码（推荐 ` + "`encoding=\"utf-8\"`" + `）。
- 外部命令调用用 ` + "`subprocess.run([...], check=True)`" + `，不要 ` + "`shell=True`" + ` 拼字符串。
- 不写用不到的抽象；先让脚本能读懂、能改。

## 依赖与环境

- 依赖写进 ` + "`requirements.txt`" + `（或 ` + "`pyproject.toml`" + `）并固定版本；能不加包就不加。
- 不把虚拟环境、缓存（` + "`__pycache__`" + `、` + "`.venv`" + `）提交进仓库。

## 数据与安全

- **不要提交密钥**；用环境变量或 ` + "`.env`" + `（并在 ` + "`.gitignore`" + ` 中排除）。
- 删除、覆盖文件等破坏性操作要先确认，或写成 ` + "`--dry-run`" + ` 优先。
- 处理别人的数据时不要静默修改原文件，输出到新文件或先备份。

## 工作方式

- 需求不清楚先问。
- 收尾给出**怎么跑、跑出什么算对**的具体命令。
`,
	},
	{
		ID:      "builtin-native",
		Name:    "C++ / 原生项目",
		Builtin: true,
		Body: `# AGENTS.md — {{NAME}} 协作约定

## 提交纪律

- **改完就提交**，用 Conventional Commits。
- 一次提交只做一件事；构建配置改动与功能改动分开提交。
- 提交前确保**能编译通过**（不留编译不过的中间态）。

## 代码

- 跟随既有代码风格与命名约定，不要另起一套。
- **优先用标准库与已有依赖**，不要为了小功能引入新库。
- 头文件里不要 ` + "`using namespace`" + `；能用前向声明就不要 include。
- 资源管理遵循 RAII；**不写裸 ` + "`new` / `delete`" + `**，用 ` + "`std::unique_ptr` / `std::vector`" + ` 等。
- 不用 C 风格强制转换；用 ` + "`static_cast` / `reinterpret_cast`" + ` 并说明理由。
- 编译警告尽量清零，尤其是 ` + "`-Wall -Wextra`" + ` 下的警告。

## 平台与兼容

- 明确写出目标平台与编译标准（如 C++17 / MSVC 2022 / Windows 10+）。
- 平台相关代码要隔离在单独文件里，不要散落在业务逻辑中。
- 不硬编码绝对路径；路径用配置文件或命令行参数传入。

## 构建

- 构建脚本要可重复执行；产物目录（` + "`build/` / `out/`" + `）不提交。
- 改了构建配置（CMakeLists / vcxproj）要在提交信息里说明。

## 工作方式

- 需求不清楚先问。
- 收尾给出**怎么构建、怎么运行、看到什么算对**的具体步骤。
`,
	},
}
